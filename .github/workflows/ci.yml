name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  check:
    name: fmt + clippy + test
    runs-on: [self-hosted, linux, x64, arch]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          submodules: recursive
          # Deploy key for the private strivo-plugins submodule. Empty
          # on public forks → checkout falls back to GITHUB_TOKEN and
          # the private submodule is skipped. See PLUGINS_PRIVATE.md.
          ssh-key: ${{ secrets.STRIVO_PLUGINS_DEPLOY_KEY }}

      - name: Toolchain
        run: |
          rustup show active-toolchain || rustup default stable
          rustup component add rustfmt clippy

      - name: Cache cargo registry / git
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: fmt
        run: cargo fmt --all -- --check

      - name: clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: test
        run: cargo test --workspace --release --all-targets --locked

      - name: build
        run: cargo build --release --locked
